/**
 * Optional placeholder. The assignment allows using Workers or Durable Objects
 * for coordination. I'm using the Worker + DO approach to keep things simple.
 *
 * If you later enable Cloudflare Workflows, you can move the "load → LLM → save"
 * logic from index.ts into a Workflow entrypoint.
 */

// (intentionally empty for this student build)
export {};